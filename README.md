# thibault

## Kids Story App

See **[kids-stories/README.md](kids-stories/README.md)** for **Storytime** — an app to create illustrated children's stories with AI.

```bash
cd kids-stories && cp .env.example .env.local  # add OPENAI_API_KEY
npm install && npm run dev
```
