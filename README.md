# thibault

## Application Contes

Voir **[kids-stories/README.md](kids-stories/README.md)** pour **Contes** — application de création d'histoires illustrées pour enfants (interface et récits en français).

```bash
cd kids-stories && cp .env.example .env.local  # add OPENAI_API_KEY
npm install && npm run dev
```
